package muddykat.silmat.auki;

public class SuperMain {
    // Required for a jar to run when compiled, the main class that it uses to launch
    // must not extend anything.
    public static void main(String[] args){
        ApplicationControl.main(args);
    }
}
